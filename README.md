# Life of Aviation by Abel

A static educational website built with Astro, React, and Tailwind CSS.

## 🚀 Project Structure

- **src/pages**: Routes (Home, About, Contact).
- **src/content**: Markdown files for Programs and Blog posts.
- **src/components**: UI blocks (Navbar, Hero, Footer).
- **src/layouts**: Base HTML wrapper.

## 🛠️ Commands

All commands are run from the root of the project, from a terminal:

| Command                   | Action                                           |
| :------------------------ | :----------------------------------------------- |
| `npm install`             | Installs dependencies                            |
| `npm run dev`             | Starts local dev server at `localhost:4321`      |
| `npm run build`           | Build your production site to `./dist/`          |
| `npm run preview`         | Preview your build locally, before deploying     |

## 🎨 Design System

- **Primary Blue**: `#003366` (Aviation Blue)
- **Accent Coral**: `#FF6F61` (Pelican Coral)
- **Font Heading**: Playfair Display
- **Font Body**: Inter